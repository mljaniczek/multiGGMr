% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{plot_pip_heatmap}
\alias{plot_pip_heatmap}
\title{Heatmap of posterior inclusion probabilities}
\usage{
plot_pip_heatmap(pip, node_labels = NULL)
}
\arguments{
\item{pip}{A numeric array of PIPs: either \code{[p, p]} (single group)
or \code{[p, p, K]} (multiple groups, as returned by
\code{\link{pip_edges}}).}

\item{node_labels}{Optional character vector of length p with node names.
If \code{NULL}, nodes are labeled 1 through p.}
}
\value{
A \code{ggplot} object. For K > 1, faceted by group.
}
\description{
Creates a heatmap of posterior inclusion probabilities (PIP) using
ggplot2 with a viridis color scale. Only the upper triangle is shown.
}
\examples{
sim <- simulate_multiggm(K = 2, p = 8, n = 80, seed = 1)
fit <- multiggm_mcmc(data_list = sim$data_list, burnin = 200, nsave = 100)
pip <- pip_edges(fit)
plot_pip_heatmap(pip)

}
\seealso{
\code{\link[=pip_edges]{pip_edges()}}, \code{\link[=plot.multiggm_fit]{plot.multiggm_fit()}}
}
