% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pcor.R
\name{posterior_precision}
\alias{posterior_precision}
\title{Extract posterior summary of precision matrices}
\usage{
posterior_precision(fit, summary_fun = mean, chain = 1L)
}
\arguments{
\item{fit}{A \code{multiggm_fit} or \code{multiggm_fit_list} object.}

\item{summary_fun}{Function to apply across posterior draws for each
matrix entry. Default \code{mean}. Other useful choices include
\code{median} or \code{sd}.}

\item{chain}{Integer; which chain to use if \code{fit} is a
\code{multiggm_fit_list}. Default 1.}
}
\value{
A named list of K numeric matrices (each p x p). List names are
\code{"Group_1"}, \code{"Group_2"}, etc.
}
\description{
Computes a summary statistic (default: mean) of the precision matrix
draws for each group. More flexible than \code{\link{coef.multiggm_fit}}
because you can specify an arbitrary summary function.
}
\examples{
sim <- simulate_multiggm(K = 2, p = 8, n = 80, seed = 1)
fit <- multiggm_mcmc(data_list = sim$data_list, burnin = 200, nsave = 100)
# Posterior mean
omega_mean <- posterior_precision(fit)
# Posterior standard deviation
omega_sd <- posterior_precision(fit, summary_fun = sd)

}
\seealso{
\code{\link[=coef.multiggm_fit]{coef.multiggm_fit()}}, \code{\link[=posterior_pcor]{posterior_pcor()}}
}
